**נושא:** מסירת מערכת דיווח עובדים אקסיומא — גרסה 1.0 והנחיות התקנה

---

שלום רב,

אני שמח להעביר אליכם את **גרסה 1.0** של מערכת דיווח עובדים אקסיומא, מוכנה להתקנה ולעלייה לאוויר. מצורפת חבילת ההתקנה המלאה: **`AxiomaReporting-Delivery-v1.0.zip`** (26 MB).

---

## מה בנינו — סקירת הפרויקט

המערכת מחליפה את תהליך הדיווח הידני הקיים (Excel) בפלטפורמה מלאה, רספונסיבית, בתמיכת RTL מלאה בעברית, המותאמת לעבודה בארגון עם מאות עובדים ברחבי המחוזות.

### מודולים מרכזיים שהושלמו

| # | מודול | מה כלול |
|---|------|---------|
| 1 | **ניהול משתמשים ותפקידים** | 6 רמות הרשאה (מנהל מערכת, מנהל פרויקט, רכז, מפקח-צפייה, מפקח-אישור, עובד), מדיניות סיסמאות (8+ תווים, 3 ניסיונות לנעילה, היסטוריית 5 סיסמאות, רוטציה כל 90 יום), אימות דו-שלבי (TFA), איפוס סיסמה בדוא"ל |
| 2 | **ניהול עובדים והקצאות** | כרטיס עובד (כרטיס כחול), כרטיס הקצאה (כרטיס ירוק), רשימת עובדים עם ייצוא ופעולות מרוכזות, רשימת הקצאות עם 12 פילטרים |
| 3 | **טופס דיווח חודשי** | 20 שדות, 10 חוקי ולידציה עסקיים, זיהוי דוחות כפולים, בדיקת דמיון הערות (Levenshtein מנורמל), אכיפת תאריך סגירה, צירוף קבצים ברמת דוח ושורה |
| 4 | **ייבוא Excel** | אפשרות להעלאת Excel מרוכז, דוח שגיאות מפורט על המסך ובקובץ PDF בעברית RTL, דריסה חכמה (רק דוחות לא מאושרים) |
| 5 | **דשבורד ומסך סיכום** | פילטרים מדורגים דו-כיווניים, ייצוא Excel, פעולות אישור/דחייה בצובר, סיכום חודשי לעובד |
| 6 | **17 טבלאות עזר** | ניהול CRUD מלא, ייבוא Excel, בדיקת שימוש לפני מחיקה, ייחודיות מסגרת לפי סמל מוסד+שלב חינוך |
| 7 | **התראות דוא"ל** | 10 תבניות עם פרסונליזציה ("שלום {שם העובד}..."), תזכורות יומיות מתוזמנות, מנגנון retry אוטומטי לכשלי SMTP, לוג שליחות מלא |
| 8 | **אבטחה וביקורת** | הצהרת תנאי שימוש עם חתימה דיגיטלית, לוג ביקורת מלא (AuditLog) על כל שינוי, Optimistic Concurrency על עדכונים מקבילים |
| 9 | **נגישות IS 5568 / WCAG 2.1 AA** | skip-link, תיוג ARIA מלא, קוראי מסך, ניווט מלא במקלדת, RTL עברית |

### נתוני בדיקה ואיכות

- ✅ **132 בדיקות אוטומטיות** עוברות, 0 נכשלות
- ✅ **0 שגיאות בנייה**, 2 אזהרות (חבילת MailKit 4.15.1 — עדכון מינורי ניתן להתקין במועד נוח)
- ✅ כל 21 שאלות הבירור מ-`CLIENT_CLARIFICATIONS.md` נענו ויושמו
- ✅ כל דרישות המפרט המקורי (`איפון+ הערות אקסיומא.docx`) נסקרו מול הקוד ואומתו

---

## מה כולל קובץ ההתקנה שמצורף

```
AxiomaReporting-Delivery-v1.0.zip
├── README.txt                             ← התחל כאן
├── app/                                   ← אפליקציה מקומפלת (להעתיק ל-IIS)
├── database/
│   ├── schema.sql                         ← סקריפט SQL מלא ליצירת סכמה
│   └── seed-data/                         ← סקריפטי Python לייבוא נתוני בסיס
├── config/
│   └── appsettings.Production.template.json  ← תבנית קובץ הגדרות
└── docs/
    ├── CLIENT_DELIVERY.md                 ← מדריך התקנה מלא בעברית
    ├── DEPLOY_CHECKLIST.md                ← רשימת תיוג
    └── OPERATIONS.md                      ← מדריך תפעול שוטף
```

---

## שלבי ההתקנה — סיכום (פירוט מלא ב-`docs/CLIENT_DELIVERY.md`)

### שלב 1 — הכנת השרת

- [ ] Windows Server 2019+ עם עדכוני אבטחה
- [ ] התקנת IIS עם כל ה-features הנדרשים
- [ ] התקנת **.NET 8 Hosting Bundle** (מאתר מיקרוסופט הרשמי)
- [ ] התקנת **SQL Server Express 2019+** עם אפשור TCP/IP על פורט 1433
- [ ] אישור SSL עבור הדומיין (Let's Encrypt חינמי דרך win-acme, או CA ארגוני)

### שלב 2 — הקמת מסד הנתונים

```sql
CREATE DATABASE AxiomaReporting COLLATE Hebrew_CI_AS;
ALTER DATABASE AxiomaReporting SET RECOVERY SIMPLE;
```

יצירת משתמש SQL ייעודי בהרשאות מינימליות (`AxiomaWeb`) — הוראות מלאות בסעיף 3.3 של המדריך.

הרצת סקריפט הסכמה:
```powershell
sqlcmd -S .\SQLEXPRESS -d AxiomaReporting -E -i database\schema.sql
```

### שלב 3 — פריסת האפליקציה

- [ ] העתקת תוכן `app/` לתיקייה `C:\inetpub\AxiomaReporting\`
- [ ] עותק של `config/appsettings.Production.template.json` לתיקיית האפליקציה, שינוי שם ל-`appsettings.Production.json`, ומילוי סיסמת ה-DB
- [ ] מתן הרשאות `Modify` לתיקייה `wwwroot\uploads\` עבור זהות ה-App Pool

### שלב 4 — הגדרת IIS

- [ ] יצירת App Pool בשם `AxiomaReporting` — No Managed Code, AlwaysRunning, Idle Timeout = 0
- [ ] יצירת Site מקושר ל-App Pool, עם HTTPS Binding ואישור SSL
- [ ] הגדרת משתנה סביבה `ASPNETCORE_ENVIRONMENT=Production`
- [ ] כלל URL Rewrite להפניית HTTP → HTTPS

### שלב 5 — הפעלה ראשונית

1. גלישה ל-`https://your-domain/` — מסך כניסה נטען
2. כניסה ראשונה: **קוד עובד = `ADMIN`**, **סיסמה = `Admin123`**
3. שינוי סיסמה מאולץ (בחרו סיסמה חזקה 8+ תווים)
4. אישור תנאי שימוש
5. הגדרת שרת דוא"ל: `/Admin/EmailServerSettings` → שליחת מייל בדיקה
6. העלאת לוגו ארגוני: `/Admin/Branding`
7. פרסום תנאי שימוש סופיים: `/Admin/TermsOfUse`
8. פתיחת חודש דיווח ראשון: `/Admin/ReportingMonths`
9. ייבוא טבלאות עזר: `/Lookup?tableName=<name>` עבור כל טבלה (מחוזות, יישובים, מוסדות וכו')
10. הוספת עובדים והקצאותיהם

### שלב 6 — בדיקת תקינות

רשימת 10 בדיקות מקוצרת נמצאת ב-`docs/DEPLOY_CHECKLIST.md`. מומלץ להריץ אותן לאחר כל פריסה.

---

## נושאים להמשך

### תחזוקה שוטפת

מדריך תפעול מלא בקובץ `docs/OPERATIONS.md` כולל:
- גיבוי יומי אוטומטי של DB + תיקיית קבצים (`D:\backups\`)
- ניטור דרך `/Admin/NotificationLogs` ו-`/Admin/AuditLog`
- תרחישי חירום: שחזור admin נעול, כשל SMTP, שחזור גרסה

### הגבלות ידועות (מוזכרות גם ב-`OPERATIONS.md §9`)

1. **ClosedXML v0.105** — באג ידוע בקריאת Excel עם רשימות ולידציה >255 תווים. פתרון: לשמור את הקובץ מחדש ב-Excel לפני העלאה.
2. **Retry של מייל ללא קובץ מצורף** — אם שליחת מייל עם PDF נכשלת, הניסיון החוזר שולח את הטקסט המלא בגוף ההודעה ללא ה-PDF (זה מקרה נדיר).
3. **שני הזדמנויות מומלצות להמשך** (לא חוסמות עלייה לאוויר):
   - תת-תפריט "ההקצאות שלי" עם 2 אייקונים (נוח אבל לא הכרחי)
   - עורך rich-text לגוף תבניות דוא"ל (כרגע textarea)

### מומלץ לבצע בשבועיים הראשונים

- [ ] להכין משתמשי Admin/PM נוספים (לא להסתמך על חשבון אחד)
- [ ] לייבא את כל טבלאות העזר מקבצי הלקוח (`טבלאות.xlsb` מצורף)
- [ ] להכשיר משתמשי קצה בטופס הדיווח (יש צילומי מסך בתיקייה `docs/screenshots/` לצורך מצגת הדרכה)
- [ ] להפעיל TFA לתפקידי Admin/PM בסביבה (`/Admin/SystemConstants` → `TfaEmailEnabled = true`)
- [ ] לבצע שחזור גיבוי לראשונה כדי לוודא שהמערכת כולה מתאוששת

---

## תמיכה

- **מסמך התקנה מלא:** `docs/CLIENT_DELIVERY.md`
- **רשימת תיוג להתקנה:** `docs/DEPLOY_CHECKLIST.md`
- **מדריך תפעול שוטף:** `docs/OPERATIONS.md`
- **מבנה DB מלא:** `docs/CLIENT_DELIVERY.md §10`
- **כל קבועי המערכת וטוקנים בתבניות:** `docs/CLIENT_DELIVERY.md §11`

אני זמין לכל שאלה, תמיכה בעת ההתקנה, או בדיקה משותפת של הסביבה הייצורית. מומלץ לתאם חלון התקנה של כ-3 שעות לעלייה הראשונית (כולל ייבוא נתוני בסיס).

בברכה,

*[שם שולח]*
*פרויקט אקסיומא — דיווח עובדים*
*2026-04-23*
