# ניתוח פערים מלא — מערכת דיווח פעילות (Site&Sound)

_נוצר: 2026-07-06. מקורות: אפיון (איפון+ הערות אקסיומא), משוב בטא 17.05.26, בדיקת פרויקט (6), QA 30.06.26._

**סיכום:** 152 דרישות נבדקו מול הקוד — 100 ממומשות, 35 חלקיות, 17 חסרות, 0 לא ברורות.

## A. Auth & Security

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| A1 | Password strength >=8 chars, letters+digits; beta: also uppercase + symbol + no same char repeated >=2 in a row | ✅ IMPLEMENTED | PasswordService.IsPasswordStrong (src/AxiomaReporting.Infrastructure/Services/PasswordService.cs:15-28) requires length>=8, hasUpper, hasLower, hasDigit, hasSymbol, and rejects any adjacent repeated char (case-insensitive Zip). Enforced in AccountController.ChangePassword:219 and ResetPassword:341.  | S |
| A2 | Lock account after 3 failed login attempts | ✅ IMPLEMENTED | AuthService.cs:14 MAX_FAILED_ATTEMPTS=3. RecordFailedLoginAsync (:90-101) increments FailedLoginAttempts and sets StatusId=Locked at >=3. ValidateLoginAsync:35 rejects Locked accounts with Hebrew message; audit 'Auth.Lockout' logged. | S |
| A3 | Password history: last 5, cannot reuse | ✅ IMPLEMENTED | AuthService.cs:15 PASSWORD_HISTORY_COUNT=5. IsPasswordInHistoryAsync (:112-121) checks last 5 via BCrypt.Verify. AddPasswordToHistoryAsync (:123-143) trims entries beyond 5. Enforced in ChangePassword (AccountController.cs:227) and ResetPassword (:360). Admin reset also pushes prior hash to history  | S |
| A4 | Password rotation every 3 months | ✅ IMPLEMENTED | PasswordService.cs:7 PASSWORD_EXPIRY_DAYS=90; IsPasswordExpired (:30-34) compares LastPasswordChange. PostLoginRedirectAsync (AccountController.cs:456) forces ChangePassword when expired; LastPasswordChange updated on every change (AuthService.cs:79, AccountController.cs:370). | S |
| A5 | Default username+password = ID; force change on first login | ✅ IMPLEMENTED | Login is by IdNumber (LoginDto.IdNumber, AuthService.ValidateLoginAsync matches u.IdNumber). EmployeeService.CreateAsync (src/.../Services/EmployeeService.cs:69,77,87) sets PasswordHash=Hash(IdNumber) and MustChangePassword=true. PostLoginRedirectAsync:456 forces ChangePassword when MustChangePasswo | S |
| A6 | Password visibility (eye) toggle on login, change-password, and first/forced change screens | ❌ MISSING | **פער:** Add an eye toggle button + small JS to switch input type on Login, ChangePassword (also covers forced first-change), and ResetPassword views. | S |
| A7 | TFA optional (email/phone code) | 🟡 PARTIAL | **פער:** Only the email channel exists. No phone/SMS code path. The toggle is a single global system constant, not per-user opt-in. | M |
| A8 | Session auto-logout after 30 min inactivity; 30 must be a configurable parameter | 🟡 PARTIAL | **פער:** Timeout is 8 hours, not 30 minutes, and is hardcoded. Change to 30 min and bind it to a configurable parameter (appsettings key or SystemConstant). | S |
| A9 | Forced change-password screen must NOT be bypassable by clicking the menu | ❌ MISSING | **פער:** Add a global action filter (mirroring the Terms filter) that redirects any authenticated user with MustChangePassword==true or an expired password to ChangePassword, exempting only account/logout actions. | S |
| A10 | Reset password from employee list -> set password back to ID and force change again; fix prior 404 bug | ✅ IMPLEMENTED | EmployeeController.ResetPassword POST (src/.../Controllers/EmployeeController.cs:255-295) sets PasswordHash=Hash(IdNumber), MustChangePassword=true, clears lockout, pushes old hash to history, audit-logs. Employee/Index.cshtml:394 posts to Url.Action('ResetPassword','Employee', id) — a real route, s | S |
| A11 | Forgot-password email flow works and email is actually received; reset link not broken/wrapped in Outlook | 🟡 PARTIAL | **פער:** Only remaining unknown is actual SMTP/mail delivery, which is environment/config-dependent and not verifiable from code. The reset link is already rendered as a clickable anchor, so no template change is needed for the Outlook concern. | M |
| A12 | After forgot-password link change, must NOT immediately re-force another change / must not allow same password | 🟡 PARTIAL | **פער:** Since the user already chose their own password via the reset link, set user.MustChangePassword = false at AccountController.cs:369 so they are not forced to change again at next login. (Note: this is the opposite of the admin-reset flow, which should keep MustChangePassword=true.) | S |
| A13 | All validation/error messages in Hebrew (no English/gibberish) | ✅ IMPLEMENTED | Within the auth surface all messages are Hebrew: AuthService.cs:33/36/39/53/55 login errors; AccountController.cs strong-password (:21-25), TFA (:73,115), reset/forgot (:264,327,362,375) all Hebrew; LoginDto.cs and ChangePasswordDto.cs ErrorMessage attributes are Hebrew ('שדה חובה', 'הסיסמאות אינן ת | S |
| A14 | Forgot/reset flow: reset first login should require change; no gibberish on ID entry | ✅ IMPLEMENTED | ResetPassword now sets MustChangePassword=true (AccountController.cs:369) so a reset-then-login DOES force a change. ForgotPassword accepts idNumber and returns a generic Hebrew confirmation ('אם נמצאה כתובת מייל...' :263-264) / Hebrew invalid-token message (:327) — no gibberish/English on ID entry. | S |

## B. Terms of Use / Privacy

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| B1 | Terms-of-use notice on first login; admin can edit terms text | ✅ IMPLEMENTED | Global gate RequireTermsAcceptedFilter.cs:65-95 registered in Program.cs:36 redirects unaccepted users to Account/TermsOfUse; PostLoginRedirectAsync forces terms screen post-login (AccountController.cs:439-454); acceptance recorded with audit+IP in AcceptTerms (AccountController.cs:151-182); admin e | S |
| B2 | Add a Privacy Policy page/tab visible to ALL users (not just first login) | 🟡 PARTIAL | **פער:** Expose a Privacy Policy link visible to ALL authenticated users (footer link in _Layout.cshtml:140-144 or the user dropdown at _Layout.cshtml:97-106) pointing to Home/Privacy, and replace the single-sentence placeholder in Privacy.cshtml with real policy content. | S |

## C. Navigation / Layout / Branding

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| C1 | Role-based top menu (Admin sees reporting/dashboard/admin; employee sees only reporting; office-non-admin sees a reduced set) | 🟡 PARTIAL | **פער:** Confirmed gap: the 'הקצאות' link (_Layout.cshtml lines 52-54) and 'ראשי' (32) are NOT role-gated, so employees (role 6) still see הקצאות, contradicting 'employee sees ONLY reporting'. AllocationsController.cs is decorated only with [Authorize] (line 13), no role restriction. Note: BuildScopedAllocationQueryAsync does scope an Employee to a.UserId == currentUserId (lines 129-130) and Details redirects employees to a read-only MyAllocations view rather than EditAllocation (54-57), so the exposure is a stray read-only link rather than full management access — but the link should still be gated to office roles (1/2/3). | S |
| C2 | Rename 'דיווח שעות' → 'פעילות חודשית' everywhere | ✅ IMPLEMENTED | Menu uses 'פעילות חודשית' (Views\Shared\_Layout.cshtml lines 37,43); footer/title say 'מערכת דיווח פעילות חודשית' (_Layout.cshtml 6,142). Grep for the exact phrase 'דיווח שעות' across Views returns no hits. | S |
| C3 | 'שלום, <name>' shown and moved to the LEFT of the bar | ✅ IMPLEMENTED | Views\Shared\_Layout.cshtml lines 92-96: user dropdown 'שלום, @User.FindFirst("FullName")' sits in <ul class='navbar-nav me-auto app-user-nav'>. Parent nav uses justify-content-between (line 27) and me-auto pushes this group to the inline-end (visual left in RTL). | S |
| C4 | Move Home + Logout icons to the LEFT of the bar | 🟡 PARTIAL | **פער:** Confirmed: Home is NOT on the left. 'ראשי' sits in the right-side app-main-nav (line 32) and the brand/home logo (SiteLogo component) is on the far right (19-22). The left app-user-nav cluster (92-114) contains only the user dropdown and logout — no Home icon/link. Add a Home affordance to the left cluster. | S |
| C5 | Menu order (RTL): ניהול, ראשי, עובדים, הקצאות, חודשי דיווח, דשבורד דיווחים | 🟡 PARTIAL | **פער:** Confirmed: order does not match the BETA sequence. Required rightmost-first is ניהול, ראשי, עובדים, הקצאות, חודשי דיווח, דשבורד — but ניהול is last in markup (rendered leftmost) and דשבורד/הקצאות/עובדים are out of sequence. Reorder the <li> elements. | S |
| C6 | Graphical menu/flow on the landing page (TEST: 'still no graphical menu') | ❌ MISSING | **פער:** Confirmed absent after full re-read. Build a graphical/tile-based, role-aware home menu (quick-access cards to reporting, dashboard, employees, allocations, admin). | M |
| C7 | Rebrand: replace every 'אקסיומא' with 'סייט אנד סאונד'/'סייט&סאונד'; logo = Site&Sound only | 🟡 PARTIAL | **פער:** Confirmed 'אקסיומא' remnants in LIVE (non-migration) source: (1) Views\Home\Index.cshtml line 5 'ברוכים הבאים למערכת דיווח אקסיומא' — visible to every user on landing; (2) Infrastructure\Services\PdfReportService.cs line 67 PDF footer 'מערכת דיווח עובדים אקסיומא'; (3) Views\Admin\EmailServerSettings.cshtml line 59 placeholder 'מערכת אקסיומא'; (4) Infrastructure\Data\SeedData.cs email templates 'מערכת אקסיומא'; (5) stale wwwroot\images\site-logo.svg — aria-label/title/text all say 'אקסיומא' (lines 2,3,11). Also historical Migrations seed rows (e.g. 20260412134615_AddReminderLogs.Designer.cs, 20260412154401_...) but those are immutable migration history. Replace the live remnants and delete/replace the stale SVG. (Tests\Ui\Playwright\WebServerFixture.cs line 88 contains 'אקסיומא' only in a local folder path, not user-facing.) | S |

## D. Employee Card (Blue Card)

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| D1 | Separate User-details screen from Allocation-details screen | ✅ IMPLEMENTED | Views/Employee/Form.cshtml renders ONLY blue-card user fields (פרטי עובד card). Allocations are a distinct screen: EmployeeController.Allocations() (line 428) redirects to the separate AllocationsController; AllocationForm.cshtml / Allocations.cshtml are separate views. Employee list (Index.cshtml)  | S |
| D2 | Internal user number shown gray/read-only | ❌ MISSING | **פער:** Add a disabled/gray read-only field on the employee card (edit mode) displaying the internal system user number (User.Id). | S |
| D3 | Rename 'מספר עובד' → 'קוד עובד' everywhere | ✅ IMPLEMENTED | Form.cshtml:37 label 'קוד עובד'; EmployeeDto.cs:12 Display 'קוד עובד'; ExportExcel header 'קוד עובד' (EmployeeController.cs:742). No stray 'מספר עובד' in the employee card; remaining 'מספר עובד/ים' hits are batch-import views meaning 'number of employees', not the field label. | S |
| D4 | 'עובד מדווח' (reporting employee) checkbox — meaning + behavior | ✅ IMPLEMENTED | Form.cshtml:127-131 checkbox bound to EmployeeDto.IsReportingEmployee; persisted in EmployeeService.cs:81,144. Behavior is real: ReminderService.cs:73 and DashboardFilterService.cs:152,222 filter reminders/missing-report lists to IsReportingEmployee only. Minor: Form.cshtml:229 JS references a non-e | S |
| D5 | Username = ID (ת.ז), not email | ✅ IMPLEMENTED | User entity (User.cs) has no Username property and no email-based login; authentication/lookup is by IdNumber (AccountController.cs:268 u.IdNumber == idNumber). Default credentials are the ID per A5 seed logic. Login is by ת.ז. | S |
| D6 | Password not shown on the card | ✅ IMPLEMENTED | Form.cshtml contains no password input; EmployeeDto has no password field. Password is set to the ID at creation and changed via ChangePassword/ResetPassword only (EmployeeController.ResetPassword sets BCrypt(IdNumber)). | S |
| D7 | Add Notes free-text field to employee | ✅ IMPLEMENTED | User.Notes (User.cs:21); EmployeeDto.Notes (max 2000); Form.cshtml:119-123 textarea 'הערות'; persisted in EmployeeService. Also surfaced in list filter/sort and Excel export (col 15). | S |
| D8 | Add 'יום מנוחה' rest-day — only Fri/Sat/Sun allowed | ✅ IMPLEMENTED | SelectListProviders.RestDayOptions = ראשון(0)/שישי(5)/שבת(6) only (SelectListProviders.cs:15-21); server-side allowlist UserValidator AllowedRestDays={0,5,6} with Hebrew message (UserValidator.cs:20,55-57); Form.cshtml:108-117 dropdown bound to RestDay. | S |
| D9 | Add 'דיווח עתידי' (future reporting) checkbox at employee level | ✅ IMPLEMENTED | User.AllowFutureReporting (User.cs:20); EmployeeDto.AllowFutureReporting; Form.cshtml:134-138 checkbox 'אפשר דיווח עתידי'; persisted in EmployeeService. | S |
| D10 | Israeli ID validity check | ✅ IMPLEMENTED | UserValidator.cs:34-36 Must(IsraeliIdValidator.IsValid) with Hebrew message; client-side attribute data-val-israeliid on Form.cshtml:45. | S |
| D11 | Phone validity check | ✅ IMPLEMENTED | UserValidator.cs:16-17,38-40 IsraeliPhoneRegex (landline+mobile prefixes), optional field, Hebrew message; client-side data-val-israeliphone on Form.cshtml:70. | S |
| D12 | Employee code = digits only | ✅ IMPLEMENTED | EmployeeDto.cs:11 RegularExpression ^\d+$; UserValidator.cs:24-26 Matches(^\d+$) with Hebrew message; Form.cshtml:38 input inputmode=numeric pattern=\d*. | S |
| D13 | Roles lookup table (תפקידים) | 🟡 PARTIAL | **פער:** Expose the EmployeeRole (תפקידים) table in the generic lookup management screen (or a dedicated CRUD screen) so admins can create/edit/delete role values. | S |
| D14 | Employee status table values in Hebrew | ✅ IMPLEMENTED | UserStatus seeded with DescriptionHebrew פעיל/לא פעיל/נעול (SeedData.cs:57-59); displayed via DescriptionHebrew ?? Name in dropdown (EmployeeController.cs:810) and Excel export (line 771). Migration 20260430110451_AddHebrewDescriptionsToRolesAndStatuses added these. | S |
| D15 | Page titles: color should be background, not part of title text (no 'כרטיס כחול/ירוק' in title) | ✅ IMPLEMENTED | Form.cshtml:5 title is 'עריכת עובד'/'הוספת עובד' with no color word; the blue is applied as card styling (card border-primary + card-header bg-primary, Form.cshtml:31-32). The literal 'כרטיס כחול/ירוק' text no longer appears in the employee card title. | S |
| D16 | On file upload add a 'description' column | ✅ IMPLEMENTED | DocumentAttachment.Description column added via migration 20260510111431_AddDocumentAttachmentDescriptionColumn; upload form has 'תיאור' input (Form.cshtml:172-176), server binds/saves it (EmployeeController.UploadAttachment lines 356-370), and the attachments table renders a 'תיאור' column (Form.cs | S |

## E. Allocation (Green Card)

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| E1 | Rename "פרטי פרויקט" → "פרטי הקצאה" | ✅ IMPLEMENTED | AllocationForm.cshtml:41 card header reads "פרטי הקצאה"; the whole card is titled accordingly. No stale "פרטי פרויקט" in the allocation form. | S |
| E2 | Terminology: annual/monthly employment scope + "משך תפוקה" | 🟡 PARTIAL | **פער:** Rename labels from "היקף פעילות" to spec-mandated "היקף העסקה" (שנתי/חודשי/יומי) across DTO Display attributes, AllocationForm, AllocationList headers/filters and the Excel export header array. | S |
| E3 | משך תפוקה: add word "דקות"; values 0.5,1,1.5,2,2.5,3 and "ללא הגבלה"; multi-select | 🟡 PARTIAL | **פער:** Add a "ללא הגבלה" checkbox to the create/edit form's משך תפוקה group and add the word "דקות" to the label/value display. | S |
| E4 | Add "היקף העסקה יומי" — up to 9 hours or "ללא הגבלה" | ✅ IMPLEMENTED | DailyEmploymentScope field with "ללא הגבלה" checkbox nulling the value (AllocationForm.cshtml:85-98,273-286); AllocationValidator.cs:46-48 enforces InclusiveBetween(0,9). Only caveat: label reads "היקף פעילות יומי" not "העסקה" (same wording issue as E2). | S |
| E5 | מחוז, תוכנית, מגזר — allow multi-select | ✅ IMPLEMENTED | ProgramIds multi-select (AllocationForm.cshtml:55), DistrictIds multi (155), SectorIds multi (166); backing lists in AllocationDto.cs:55-57. Rendered as chip multi-selects via Choices.js. | S |
| E6 | Add Notes free-text field to allocation | ✅ IMPLEMENTED | Allocation.Notes property (Allocation.cs:18), AllocationDto.Notes (AllocationDto.cs:47-49), textarea in form (AllocationForm.cshtml:139-140). | S |
| E7 | Monthly-rows field redundant on this screen; belongs in dashboard (no limit) | ❌ MISSING | **פער:** Remove the monthly-rows input from the allocation create/edit screen and express row usage in the reports dashboard instead. Confirm with PM given tension with business-rule #16 (per-allocation row limits) before removing the underlying field. | S |
| E8 | On choosing Program/Project auto-populate ALL related field values per program's code-tables; editable afterwards | ❌ MISSING | **פער:** Add per-program association tables (program→districts/sectors/localities/subjects/domains/educational-programs/…) + admin CRUD to define them, plus JS to auto-fill/replace the שיוכים selects on program selection while keeping them editable. Per QA #4, frameworks must remain filtered by employee allocation, not program-conditioned. | L |
| E9 | "תוכנית" field at top of allocation, first-from-right before מחוז; selecting it fills "שיוכים" | 🟡 PARTIAL | **פער:** Move תוכנית to be first-from-right before מחוז (beta [B7] line 12 / [B19]) and wire program-selection to fill the שיוכים selects (depends on E8). | M |
| E10 | Add-allocation entry point ON the allocations screen (not only via employee screen) | 🟡 PARTIAL | **פער:** Add an add-allocation action on the standalone allocations screen that first prompts the user to pick an employee, then routes to CreateAllocation. | S |
| E11 | Add "סוג דיווח" field to allocation — optional, auto-shown; appears in report screen and output col B (right of ת.ז) | ❌ MISSING | **פער:** Add nullable ReportTypeId to Allocation + AllocationDto + an optional select on AllocationForm, flow the value to the report screen, and place "סוג דיווח" as export column B immediately right of ת.ז. | M |
| E12 | Monthly/annual scope: no digits after decimal point | ✅ IMPLEMENTED | Inputs use step=1 + whole-number-field JS stripping non-digits (AllocationForm.cshtml:71-81,288-291); DTO uses DisplayFormat "{0:0}" (AllocationDto.cs:19,24,29); AllocationValidator.cs:26-36,59-63 BeIntegerOrNull; list displays via Whole() truncation (AllocationList.cshtml:110). | S |
| E13 | Multi-select edit UX must be clear (how to update multi-selects) | ✅ IMPLEMENTED | Choices.js is globally initialized on every select[multiple] (_Layout.cshtml:158-173) giving tag-chip UI with removeItemButton and type-to-search; programs select owns a dedicated Choices instance (AllocationForm.cshtml:304-315). Caveat: the stale hint text "להחזיק את מקש הבחירה המרובה" (AllocationF | S |
| E14 | Framework display in allocation = יישוב + סמל + שם מסגרת | ❌ MISSING | **פער:** Add locality linkage to Framework (or resolve יישוב via Institution) and render the framework display as "יישוב + סמל + שם מסגרת" in the allocation form (and list if required). | M |
| E15 | Clarify source of "הערות" data shown in allocation list | ✅ IMPLEMENTED | The Notes column in the allocation list is bound to the allocation's own Notes field: AllocationList.cshtml:263 header and 292 cell render a.Notes; filter also targets a.Notes (AllocationsController.cs:224-225). This resolves the beta [B14] question — the value comes from the allocation-level Notes  | S |

## F. Employee List

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| F1 | Filter/search by every column in the filter bar | 🟡 PARTIAL | **פער:** Four visible/sortable columns have no filter: מייל (Email), טלפון (Phone), עובד מדווח (IsReportingEmployee), and the employee job role תפקיד (employeerole, distinct from the system-role roleId filter). Model EmployeeListFilterModel.cs lacks Email/Phone/IsReportingEmployee/EmployeeRole fields. Spec/TEST Sheet1 #15 requires every column filterable. | S |
| F2 | Sort by every column via header click | ✅ IMPLEMENTED | Index.cshtml header cells (lines 320-373) wrap every data column in SortLink(...) with aria-sort, and ApplyEmployeeSort (EmployeeController.cs:968-1019) handles all keys: employeecode, idnumber, firstname, lastname, employeerole, userrole, status, isreportingemployee, locked, email, phone, restday,  | S |
| F3 | Row action buttons on the RIGHT (visible without horizontal scroll) | ✅ IMPLEMENTED | Index.cshtml line 315 comment 'Actions column moved to FIRST position (right side in RTL) per client Fix #13'; the actions <th> is the first column (line 316) and the action buttons cell is first in each row (lines 383-436). In the dir=rtl table the first column renders rightmost, so actions are vis | S |
| F4 | Filter state persists across actions (e.g. disable then refresh keeps filter) | ✅ IMPLEMENTED | Index.cshtml BuildUrl/SortLink/PageLink/ExportLink (lines 46-81) carry all filter params through pagination, sort and export. Row-action forms (ResetPassword line 394, UnlockAccount 412, DeleteEmployee 425) append @Context.Request.QueryString to the POST action and also emit hidden filter inputs. Co | S |
| F5 | Bulk operations: change status, change allocation | ✅ IMPLEMENTED | Index.cshtml bulk bar (lines 287-309) with select-all JS (508-530). BulkAction (EmployeeController.cs:612-630, [AdminOrPM]) bulk-updates StatusId; BulkAddAllocation (637-667) bulk-adds an allocation for a chosen project to the selected employees. | S |
| F6 | 'נעול' (locked) column visible in list | ✅ IMPLEMENTED | Index.cshtml header 'נעול' (lines 344-346, sortable) and cell (line 457) showing כן/לא via isLocked = StatusId==3 \|\| FailedLoginAttempts>=3 (line 381). Also a 'רק חשבונות נעולים' filter (lines 246-252) and LockedOnly filter logic (EmployeeController.cs:120-121). | S |
| F7 | Reflect employee Notes field in list | ✅ IMPLEMENTED | Index.cshtml 'הערות עובד' column header (lines 371-373) and cell rendering emp.Notes (line 471). Notes are also filterable (line 242) and sortable, and included in the Excel export (EmployeeController.cs:756,780). | S |
| F8 | Export list to Excel | ✅ IMPLEMENTED | 'ייצוא אקסל' button (Index.cshtml line 98) linking to ExportLink() which preserves current filter/sort. ExportExcel action (EmployeeController.cs:731-794) builds an XLWorkbook with Hebrew headers (קוד עובד, מספר זהות, שם, תפקיד, סטטוס, עובד מדווח, מייל, טלפון, דיווח עתידי, פרויקטים, מחוזות, תוכניות, | S |
| F9 | Rename action 'השבתה' → 'לא פעיל' (BETA #B22/F22) | ❌ MISSING | **פער:** Rename deactivate button (and ideally confirm/success text) from 'השבתה' to 'לא פעיל' per BETA feedback. Confirmed absent after searching the view and controller. | S |
| F10 | New employee: can add allocations from card and from list; add saves | ✅ IMPLEMENTED | From the list each row has a 'הקצאות' button (Index.cshtml lines 387-388) → Employee/{id}/Allocations, plus bulk 'הוסף הקצאה' (line 304). From the card, Edit form shows 'ניהול הקצאות' (Form.cshtml lines 142-149). CreateAllocation GET/POST (EmployeeController.cs:475-513) renders AllocationForm and pe | S |
| F11 | Employee must be able to see their own allocations | ✅ IMPLEMENTED | MyAllocationsController.cs ([Authorize(Roles="6")] = Employee) Index (lines 30-57) lists the signed-in user's active allocations, Details (59-66) shows one, ExportExcel (69-115) exports them; MyAllocationsQuery scopes strictly to a.UserId == userId (line 140). Views/MyAllocations/Index.cshtml and De | S |

## G. Reporting Form (Online)

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| G1 | 20-field report row per spec table | ✅ IMPLEMENTED | ReportRow entity has all fields (src/AxiomaReporting.Core/Entities/ReportRow.cs). Grid header renders תאריך/משך/מחוז/ישוב/מסגרת/תוכנית/תחום/נושא1/נושא2/קוד דיון/מסקנה-כיתה/מסקנה-מסגרת/מסקנה-מיקום/שכבה/כיתה/הערות (Views/Report/Index.cshtml:266-285) plus employee ת.ז/שם/קוד עובד in header card (172-17 | S |
| G2 | Required-field validation for all mandatory fields | ✅ IMPLEMENTED | ReportValidationService.ValidateRowAsync lines 51-60 validate each required field with Hebrew messages; required set is configurable via SystemConstants 'RequiredReportFields' (GetRequiredFieldsAsync, lines 314-327). View renders '*' markers and aria-required (Index.cshtml Req/Star helpers lines 50- | S |
| G3 | Dates: current/past months; future only if future-reporting enabled | ✅ IMPLEMENTED | ReportValidationService.cs:66-73 — allowFuture = employee.AllowFutureReporting && month.AllowFutureReporting; blocks meetingDate>today unless allowFuture; blocks meetingDate>reportingMonthEnd otherwise. | S |
| G4 | No exceeding monthly row allocation | ✅ IMPLEMENTED | ValidateSubmitAsync ReportValidationService.cs:117-120 compares allocationGroup.Count() to allocation.MonthlyRowAllocation and errors on exceed. | S |
| G5 | No exceeding 9 daily hours unless ללא הגבלה | 🟡 PARTIAL | **פער:** An allocation with DailyEmploymentScope=NULL bypasses the daily cap instead of defaulting to 9h. Fix: at lines 200-201 do not early-return; let limit fall through to the MaxDailyHoursDefault (9) constant when the allocation's scope is null. | S |
| G6 | No exceeding annual row allocation | ✅ IMPLEMENTED | ValidateSubmitAsync ReportValidationService.cs:127-136 counts annual rows for the allocation within the reporting year and errors if > allocation.AnnualRowAllocation. | S |
| G7 | Duplicate detection excluding the hours field (BETA #B26) | 🟡 PARTIAL | **פער:** BETA #B26 requires the duration (hours) field to be excluded from the duplicate key so rows identical except for hours are still blocked. Remove the line-263 MeetingDuration clause from the predicate. | S |
| G8 | Notes similarity %: configurable threshold | ✅ IMPLEMENTED | ValidateNotesSimilarityAsync ReportValidationService.cs:294-312 reads SystemConstant 'NotesSimilarityThresholdPercent' (default 90) and uses normalized Levenshtein similarity (329-349); a one-char change on a substantial note still exceeds 90% and is blocked. | S |
| G9 | Deadline: submit only until month's last reporting date | ✅ IMPLEMENTED | ValidateSubmitAsync ReportValidationService.cs:93-94 blocks submit when Today>month.LastReportingDate; ReportController.IsDeadlinePassed (182-186) also locks editing after deadline (except SystemAdmin override). | S |
| G10 | Cannot report on employee's rest day | ✅ IMPLEMENTED | ReportValidationService.cs:75-76 — if employee.RestDay matches meetingDate.DayOfWeek, adds error 'לא ניתן לדווח ביום המנוחה של העובד'. | S |
| G11 | Inline row editing (edit in the row, not only popup) | ✅ IMPLEMENTED | Views/Report/Index.cshtml provides per-cell inline edit (editCell/saveCellEdit lines 669-763) and full-row inline edit (inlineEditRow/saveInlineRow lines 770-851), plus 'עריכה בשורה' button (314). | S |
| G12 | Duplicate-row (שכפול שורה) feature | ✅ IMPLEMENTED | 'שכפל' button (Index.cshtml:316) and duplicateRow() JS (936-965) load the source row into the modal with rowId=0 to create a copy. | S |
| G13 | Save draft & resume (status טיוטה) | ✅ IMPLEMENTED | ReportController.Index uses _statusService.GetOrCreateDraftAsync (line 108); SaveRow calls SaveDraftAsync (314); StatusText maps 1=>טיוטה (Index.cshtml:83). Draft persists rows and reopens for editing. | S |
| G14 | Default sort by date+sequence; sortable by all columns | ✅ IMPLEMENTED | Controller orders rows by MeetingDate then SequenceNumber (ReportController.cs:127-128). All headers marked sortable with client-side sortReportTable + aria-sort (Index.cshtml:853-890). | S |
| G15 | Actions column FIRST from right in report grid (TEST Sheet4 #1) | ❌ MISSING | **פער:** Move the פעולות column to be the first column (first-from-right in RTL) in both <thead> (line 284) and <tbody> (lines 309-321). | S |
| G16 | 'שורות בעמוד' rows-per-page control: same right-side position + title across screens | 🟡 PARTIAL | **פער:** The report grid is unpaginated, so the cross-screen consistency requirement is unmet here. Either add a 'שורות בעמוד' control matching the dashboard's title/position, or confirm the report grid is intentionally exempt. | M |
| G17 | In edit show ALL selected column values (expand row so all visible) | ✅ IMPLEMENTED | inlineEditRow renders a full-width detail row (colspan) exposing every field's current value (Index.cshtml:787-812); the modal editRow() also populates all 16 fields (905-934). | S |
| G18 | מסגרת header should read 'מסגרת חינוכית' in manual report (QA #5) | ❌ MISSING | **פער:** Rename the framework column header (line 272) and the modal/inline labels (lines 421, 794) in the manual report form from 'מסגרת' to 'מסגרת חינוכית'. | S |
| G19 | Framework select shows each description TWICE (bug, QA #6) | ✅ IMPLEMENTED | Framework options are built from the allocation's junction rows loaded via AsSplitQuery (ReportController.cs:137-149), then projected to distinct {id,text} (Index.cshtml:67). Split-query loading avoids the cartesian duplication that produced the double-listing; no doubling logic present. | S |
| G20 | All combo boxes support type-to-search (QA #7) | ❌ MISSING | **פער:** Attach Choices.js (or an equivalent searchable widget) to the single-select report combos in the modal, inline-row and cell editors. | M |
| G21 | Framework value = יישוב + סמל + שם מוסד with type-ahead narrowing | 🟡 PARTIAL | **פער:** Compose the framework option text as יישוב + סמל מוסד + שם מוסד and add type-ahead narrowing on the framework combo. | M |
| G22 | Framework select filtered ONLY by employee allocation, not by program (QA #4) | ✅ IMPLEMENTED | Framework list is sourced purely from allocation.AllocationFrameworks junctions (ReportController.cs:141; Index.cshtml:25-26); no program-based filtering is applied. Scope validation also checks only the allocation's frameworks (ReportValidationService.cs:174-175). | S |
| G23 | Document attachments per-employee AND per-report; show indication/access | 🟡 PARTIAL | **פער:** Add per-row upload UI and surface/indicate employee-level (UserId) documents within the report screen; currently only report-level upload exists and per-employee docs live solely on the employee card. | M |
| G24 | Only admin can fix/edit an already-submitted report (TEST Sheet4 #3) | ✅ IMPLEMENTED | CanEditReport (ReportController.cs:602-616): SystemAdmin always allowed; StatusId 3 (PendingApproval) or 4 (Approved) locked for everyone else; only Draft/InEntry/ReturnedForCorrection editable by owner/coordinator. SaveRow/DeleteRow/Submit/UploadExcel all gate on CanEditReport. | S |
| G25 | From dashboard→edit report, provide a way back to dashboard | ✅ IMPLEMENTED | returnUrl is threaded through Index/SaveRow/Submit and validated as local (NormalizeLocalReturnUrl 644-656); view renders 'חזרה לדשבורד דיווחים' back link (Index.cshtml:129-134); Approve/Reject redirect back via RedirectBackToDashboard (624-631). | S |
| G26 | Employee with no allocation sees a proper (non-gibberish) message | ✅ IMPLEMENTED | ReportController.Index returns View('NoAllocation') with Hebrew ViewBag.Error 'אין הקצאה פעילה לעובד זה' (89-93); NoAllocation.cshtml renders a clean Hebrew alert + home link. | S |
| G27 | Report Excel export: Hebrew headers + column order identical to screen | ✅ IMPLEMENTED | DashboardController.ExportExcel (74-137) writes fully Hebrew headers ('מס"ד','ת.ז','קוד עובד','מסגרת חינוכית','קיום דיון', etc.) with RTL worksheet; SummaryExportExcel similarly Hebrew (140-185). | S |
| G28 | Let employee download their own report as Excel (TEST Sheet7 #4) | ❌ MISSING | **פער:** Add an Excel export of the employee's own report rows (Hebrew headers, column order matching the on-screen grid per beta [F24]/[F25]) accessible to the Employee role on the report screen. | M |

## H. Excel Upload (report import)

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| H1 | Mobile-friendly upload screen | 🟡 PARTIAL | **פער:** No dedicated mobile-optimized upload screen. The employee single-upload file input + submit button sit in an inner d-flex WITHOUT flex-wrap (Report/Index.cshtml:344), so on very narrow screens the file picker and button do not stack; they rely on being btn-sm/form-control-sm to fit. Batch screen is genuinely mobile-friendly; single-report inline control is only adequately responsive. No evidence of mobile-width verification. | S |
| H2 | Validate uploaded rows against employee allocation for ALL values (esp. combo tables) | ✅ IMPLEMENTED | Both importers run every parsed row through IReportValidationService.ValidateRowAsync (ReportExcelImportService.cs:101; BatchReportImportService.cs:317). ReportValidationService.ValidateAllocationScopeAsync (lines 145-184) checks District, Locality, Framework, EducationalProgram, Domain, Subject1/2, | S |
| H3 | On errors: show error list ON SCREEN + allow PDF export of errors | ✅ IMPLEMENTED | Single import: errors stored in TempData["Errors"] and rendered on the report page with a 'הורד PDF שגיאות' link (ReportController.cs:466-474; Views/Report/Index.cshtml:143-151). PDF generated via _pdfReportService.CreateErrorReport. Batch import: BatchReportImportResult.cshtml:114-142 shows an on-s | S |
| H4 | Overwrite only unapproved reports | 🟡 PARTIAL | **פער:** Batch import does NOT block appending to an Approved (StatusId 4) or PendingApproval (StatusId 3) report. Lines 371-376 only reset ImportedFromExcel/UpdatedAt and flip StatusId 1->2; an already-approved report keeps StatusId 4 while new rows are appended (line 389). This violates rule 11 'Excel upload overwrites ONLY unapproved reports' for the multi-employee batch path. Fix: add the same StatusId is 3 or 4 guard (skip/reject the row) in the batch persist loop. | S |
| H5 | Employee upload = current month only; PM can upload for locked months | ✅ IMPLEMENTED | Employee single upload is bound to the report being edited; CanEditReport (ReportController.cs:602-616) returns false once IsDeadlinePassed (past/locked month) for any non-SystemAdmin, so an employee is effectively limited to the active month. PM/admin batch import (AdminController.BatchReportImport | S |
| H6 | Prevent employee uploading a file with another employee's details | ✅ IMPLEMENTED | Single import binds all rows to the fixed report.UserId. For the client-Hebrew file format, IsRowForReportEmployee (ReportExcelImportService.cs:213-219) compares the file's employee-code cell against the report user's EmployeeCode/IdNumber and rejects mismatches with a Hebrew error (lines 90-95). Th | S |
| H7 | Do NOT auto-create a non-existent employee from an upload | ✅ IMPLEMENTED | Single import never creates users (imports into an existing report). Batch import looks up the user by EmployeeCode and, if not found, records a rejection error 'עובד עם קוד {code} לא נמצא במערכת' and skips the row (BatchReportImportService.cs:176-191) — no user creation anywhere. | S |
| H8 | No gibberish rows shown after report-Excel upload | ✅ IMPLEMENTED | Client-Hebrew import resolves every text cell to a real lookup FK via ILookupResolver (ReportExcelImportService.cs:246-268). More decisively, the report validator rejects any lookup value not in the row's allocation before persistence: ReportValidationService.cs:170-185 (District/Locality/Framework/ | S |
| H9 | Mobile file picker should not be limited to images (show docs/Downloads) | ✅ IMPLEMENTED | Both upload inputs use accept=".xlsx" (Views/Report/Index.cshtml:349; Views/Admin/BatchReportImport.cshtml:50) — no image/* restriction and no capture attribute, so mobile browsers open the document/file picker, not the camera/gallery only. | S |
| H10 | Importing previous-months reports must work | ✅ IMPLEMENTED | Batch import accepts an explicit reportingMonthId chosen from all months (incl. past) and imports without a deadline gate (AdminController.cs:1502-1516; BatchReportImportService.ImportAsync loads the selected month and processes rows). This provides a working path for previous-month imports for PM/a | S |
| H11 | Employee (regular) importing Excel must not be kicked out | ✅ IMPLEMENTED | ReportController is class-level [Authorize] (ReportController.cs:15) — an authenticated employee stays authenticated throughout. UploadExcel (ReportController.cs:436-482) has a fully graceful path: an employee owning the report passes CanEditReport (report.UserId == _currentUser.UserId, line 615); o | S |
| H12 | Batch upload of all employees/all programs for a month by admin/PM | ✅ IMPLEMENTED | Full multi-employee batch importer: IBatchReportImportService / BatchReportImportService (auto-detects header row by 'קוד עובד', maps ~18 columns via HeaderAliases, resolves lookups, picks the matching allocation per row via ResolveAllocation, validates, and creates/updates one report per employee). | S |

## I. Dashboard (reports)

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| I1 | Cascading filters; choosing מחוז surfaces all related values across fields | 🟡 PARTIAL | **פער:** On the primary reports dashboard, choosing מחוז does not dynamically narrow the full dropdown set; only sector/program cascade and only server-side on הצג. Wire the already-built FilterOptions AJAX into Index.cshtml and extend cascading to the allocation-dimension lookups. | M |
| I2 | Table empty until 'הצג' pressed; pressing 'הצג' with nothing selected must not error | 🟡 PARTIAL | **פער:** Gate initial load on a submitted flag (e.g. honor the show=1 param) so the results grid stays empty until הצג is pressed. Currently it eagerly loads all scoped data. | S |
| I3 | Export to Excel by current filters | ✅ IMPLEMENTED | DashboardController.ExportExcel (DashboardController.cs:73-137) accepts the same DashboardFilter, forces InspectorView/Approval to StatusId=4 (approved-only, rule #13), sets PageSize=10000, calls GetReportRowsAsync and streams an xlsx with RTL Hebrew headers. Index.cshtml:69 builds the export link w | S |
| I4 | 'מסך סיכום' button → summary/approval screen | ✅ IMPLEMENTED | Index.cshtml:70 '<a asp-action="Summary" ...>מסך סיכום ואישור</a>'; DashboardController.Summary (DashboardController.cs:187-199) renders Views/Dashboard/Summary.cshtml with summary stats, approve/reject and bulk-approve. | S |
| I5 | Filter dropdowns open downward and readable (start of text visible) | ✅ IMPLEMENTED | All filter controls in Index.cshtml (lines 111-164) and Summary.cshtml (57-83) are native Bootstrap <select class="form-select form-select-sm"> elements rendered inside an RTL layout; native selects open downward and show text from the start, with no custom/absolutely-positioned dropdown overlay tha | S |
| I6 | Statuses displayed in Hebrew (not English) | ✅ IMPLEMENTED | DashboardFilterService.GetReportStatusText (DashboardFilterService.cs:1022-1033) maps status ids to Hebrew ('טיוטה','בהזנה','ממתין לאישור','מאושר','הוחזר לתיקון') and is used for both detail rows (line 329) and summary rows (line 572). Index status dropdown options (Index.cshtml:127-132) and Summary | S |
| I7 | Filter persistence across reject/status changes (must not silently reset to 'טרם דווח') | ✅ IMPLEMENTED | Summary reject/approve forms post returnUrl=currentUrl (Summary.cshtml:225,311); ReportController.Approve/Reject redirect via RedirectBackToDashboard(returnUrl) with NormalizeLocalReturnUrl (ReportController.cs:408-431,624-655), returning to the same summary URL+querystring so the filter persists. S | S |
| I8 | 'נקה' clear-filters label consistent = 'נקה' | ✅ IMPLEMENTED | Index.cshtml:168 '<a href=... >נקה</a>' and Summary.cshtml:91 '<a href=... >נקה</a>' both use the label 'נקה' next to the 'הצג' submit button. | S |
| I9 | Edit button first-from-right, first column of the report grid | ✅ IMPLEMENTED | In Index.cshtml the actions column is rendered FIRST (before מס"ד) both in the header (lines 197-200 '<th>פעולות</th>') and body (lines 239-258), so in the RTL table it is the right-most/first column. Column only shows when ViewBag.CanEditDashboardRows (SystemAdmin, DashboardController:230). | S |
| I10 | 'טרם דווח' status = all employees required to report who haven't in the active period | ✅ IMPLEMENTED | StatusId==0 routes to GetMissingReportsAsync (DashboardFilterService.cs:587-631) and GetMissingReportDetailRowsAsync (359-410): resolves the active/selected reporting month (ResolveMissingReportMonthAsync:683-692), finds scoped active reporting employees/allocations that have no submitted report for | S |
| I11 | Month-range filter (from שכר month to שכר month) | ✅ IMPLEMENTED | DashboardFilter.FromMonthId/ToMonthId (DashboardFilterService.cs:33-34); ApplyMonthRangeAsync (633-656) filters reports on Year*12+Month between the two months. Index.cshtml exposes 'מחודש' and 'עד חודש' selects (lines 109-122) bound to those fields. | S |
| I12 | 'הצג דיווחים בארכיון' — clarify meaning/purpose (archive toggle) | ❌ MISSING | **פער:** Clarify the requirement with the client, then either add a 'show archived reports' flag/toggle on reports or document as out of scope. Nothing implemented today. | M |
| I13 | Documents: total-count column + access to documents; per allocation-report row a documents field to view at employee level | 🟡 PARTIAL | **פער:** Replace the boolean with a document count and add an accessible link/modal to view/download attachments per report row at employee level. | M |
| I14 | Move 'מספר שורות בעמוד' control to the LEFT of the screen | 🟡 PARTIAL | **פער:** Relocate the page-size selector to the left edge of the dashboard toolbar and standardize its title/position across screens. | S |
| I15 | 'קיום דיון' is a closed list per project, not yes/no | 🟡 PARTIAL | **פער:** Scope the discussion-code list per project/program and rename the dashboard label consistently to 'קיום דיון'. | M |
| I16 | Dashboard 'enter' should show all report rows (was broken) | ✅ IMPLEMENTED | DashboardController.Index (DashboardController.cs:39-42) calls GetReportRowsAsync which returns one DashboardReportDetailRow per ReportRow across all matching reports (DashboardFilterService.cs:213-357), including all 20+ report-row fields, joined via ReportRows with full Includes (lines 248-267). I | S |

## J. Summary / Approval Screen

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| J1 | Summary per report: total rows, total output (משך תפוקה), remaining-to-report | ✅ IMPLEMENTED | Views/Dashboard/Summary.cshtml lines 173-175 render columns שורות (r.RowCount), משך תפוקה (r.TotalDuration), יתרת שורות (r.RemainingRows). Backed by DashboardFilterService.cs: DashboardReportRow.TotalDuration (line 73, summed from rows line 773), MonthlyRowAllocation (line 74), RemainingRows compute | S |
| J2 | Approve → status אושר + confirmation email | ✅ IMPLEMENTED | ReportController.Approve (lines 405-415) calls _statusService.ApproveReportAsync then redirects. ReportStatusService.ApproveReportAsync (lines 108-144) sets StatusId=4 (Approved), ApprovedAt/ApprovedBy, and sends 'ReportApproved' email via _emailService.SendAsync with EmployeeName/MonthName. Also re | S |
| J3 | Reject → popup for reasons → status הוחזר לתיקון + email with reasons | ✅ IMPLEMENTED | Summary.cshtml reject modal (lines 300-324) with required textarea rejectionReason, opened by openReject(). ReportController.Reject (lines 417-432) validates reason then calls RejectReportAsync. ReportStatusService.RejectReportAsync (lines 146-184) sets StatusId=5 (Returned for Correction), stores R | S |
| J4 | Bulk approve controls: select-all, clear-all, multi-select checkboxes | ✅ IMPLEMENTED | Summary.cshtml lines 148-241: bulk <form asp-action="BulkApprove">, buttons 'בחר הכל' selectAll(true) and 'בטל בחירה' selectAll(false), header select-all checkbox #selectAllCb, per-row checkboxes name=reportIds (line 193), live selected count + disabled submit until >0 (JS lines 328-344). DashboardC | S |
| J5 | Missing 'employees who didn't report' screen | ✅ IMPLEMENTED | Implemented on the dashboard as a status filter rather than a standalone screen. Dashboard/Index.cshtml line 127 offers status option value=0 'טרם דווח'. DashboardFilterService routes StatusId==0 to GetMissingReportsAsync (line 164) / GetMissingReportDetailRowsAsync (line 234, 359) which resolve the | S |
| J6 | Fix approve/reject bug: previously navigated to non-existent page and sent no email; intermittent errors | ✅ IMPLEMENTED | Root causes appear addressed. Approve/Reject (ReportController 405-432) both send emails through ReportStatusService (ApproveReportAsync/RejectReportAsync send ReportApproved/ReportRejected). Navigation uses RedirectBackToDashboard (lines 624-631) with NormalizeLocalReturnUrl guarding against invali | S |

## K. Lookup Tables / Admin

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| K1 | Generic lookup CRUD: auto-code + description; add/edit; free-text search; pagination; scrollbar | ✅ IMPLEMENTED | LookupController.cs handles 15 lookup tables via generic dispatch: List (search+page+pageSize params, lines 56-73), Create (79-91), Edit (97-104), Delete (110-122). FilterAndSearch uses EF.Functions.Like on Description (200-205). Views/Lookup/List.cshtml renders id ('מזהה' auto-increment = code), de | S |
| K2 | Delete only after in-use check; confirm dialog | ✅ IMPLEMENTED | LookupController.CanDeleteItemAsync (291-349) checks ReportRows/Allocation link tables/Institutions/InspectorAssignments per table before delete; returns Hebrew reason if in use. Delete action blocks and sets TempData['Error'] (112-117). List.cshtml delete-form has JS confirm() dialog (294-301). | S |
| K3 | Only admin edits/adds lookup values | ✅ IMPLEMENTED | All mutating Lookup actions carry [Authorize(Policy=CanManageLookups)]; Program.cs line 115-116 maps CanManageLookups to RequireClaim(Role,'1') = admin only. List.cshtml gates add/edit/import/delete buttons behind isAdmin (User.IsInRole('1')). | S |
| K4 | Frameworks table: שם מסגרת/סמל columns, symbol unique per stage, duplicate error, search by name/symbol/stage/locality, import+export Excel, bulk active/inactive toggle | 🟡 PARTIAL | **פער:** Confirmed absent after re-reading the full view and controller: Frameworks.cshtml has NO search/filter form (only a plain table; contrast Views/Lookup/List.cshtml:46-71 which does have one), so no search by name/symbol/stage/locality — and Framework.cs has no locality field at all (grep for Locality/ישוב in Framework.cs = no matches). No Excel EXPORT action for frameworks (grep ExportFramework = none). No bulk active/inactive toggle — only per-row edit modal checkbox (Frameworks.cshtml:149-152). Import is buried in the data-migration tool, not offered on the frameworks screen. | M |
| K5 | Clarify distinction between רשימת מוסדות and רשימת מסגרות | 🟡 PARTIAL | **פער:** The overlap/conceptual ambiguity is a data-model design question that code cannot itself resolve. No in-app help text or documentation on Frameworks.cshtml or Institutions.cshtml explains which screen to use for what. Remains unresolved (BETA #B28). | S |
| K6 | Rename 'קוד דיון' → 'קיום דיון' | 🟡 PARTIAL | **פער:** Still 'קוד דיון' (grep confirmed) in Report/Index.cshtml:277 (grid header), :461 (form label), :799 (edit-form label); Dashboard/Index.cshtml:157 (filter label) and :216 (column header); LookupController.cs:41 (lookup admin table display name); DataMigration.cshtml:200 (column-map help). The main report form, dashboard filter/grid, and lookup admin still show the old term. | S |
| K7 | Reporting-months table: code, description, month/year picker, last-reporting-date, only one active, activating deactivates previous, future-reporting yes/no field | ✅ IMPLEMENTED | AdminController.ReportingMonths + CreateReportingMonth (month, year, description, LastReportingDate, AllowFutureReporting) lines 54-81. ActivateReportingMonth (83-102) sets all others IsActive=false then activates the target (business rule #4). ReportingMonth entity carries AllowFutureReporting tied | S |
| K8 | After a month is active, future-reporting + last-date editable only by PM/admin | ✅ IMPLEMENTED | AdminController.EditReportingMonth GET (122-146) sets LockNonAdminFields = month.IsActive && !IsAdminOrProjectManager(); POST (148-181) recomputes lockNonAdmin and, when locked, ignores inbound LastReportingDate/AllowFutureReporting keeping DB values while still allowing description/month/year. IsAd | S |
| K9 | System-constants table: editable by admin, no delete; dedupe the 90% change constant | ✅ IMPLEMENTED | AdminController.SystemConstants + UpdateSystemConstant only (356-382), both [Authorize(AdminOnly)]; there is NO delete action. SeedData.cs SeedSystemConstants (75-152) seeds a single NotesSimilarityThresholdPercent=90 (id 3, lines 95-102) — no duplicate, resolving BETA #B27. | S |
| K10 | Manage which code-tables' values belong to each project & program (cascade source for allocation auto-fill) | ❌ MISSING | **פער:** Confirmed truly absent: no entity or screen associates lookup-table VALUES (districts, frameworks, subjects, domains, classes...) to a program or project. Grep for Program{Domain,Subject,Framework,Table,Lookup,Value,Field} association entities = nothing. This is the configured data source the E8 allocation cascade needs (TEST Sheet7 #3); without it the program→field auto-population cannot be driven from configured tables. Needs new association entities + management UI. | L |
| K11 | Email server settings table | ✅ IMPLEMENTED | EmailServerSetting entity; AdminController.EmailServerSettings + SaveEmailServerSettings (500-547) [Authorize(AdminOnly)] with SmtpServer/Port/Username/Password/FromAddress/FromName/UseSsl; password preserved when blank; audit logging excludes password. View Views/Admin/EmailServerSettings.cshtml. | S |
| K12 | Email templates table (received/rejected/reminder) with 'שלום <name>' personalization | ✅ IMPLEMENTED | EmailTemplate entity; AdminController.EmailTemplates + UpdateEmailTemplate (466-496) [AdminOnly]. SeedData.cs SeedEmailTemplates (154+) seeds ReportReceived, ReportApproved, ReportRejected (with {{RejectionReason}}), ReminderNotSubmitted, ReminderNeedsCorrection, PasswordReset, TwoFactorCode, etc.,  | S |
| K13 | Inspector-assignments screen: add ת.ז + first name + last name fields | 🟡 PARTIAL | **פער:** Confirmed after full re-read of the view: no ת.ז (IdNumber) is ever displayed or entered anywhere on the screen (grep for מחק/delete and manual scan show no IdNumber column or input), and there are no separate first-name/last-name fields — only the combined name dropdown. BETA #B18/F18 asks for explicit ת.ז + first + last name fields. | S |
| K14 | Change 'delete' to a trash-can icon in all tables | 🟡 PARTIAL | **פער:** Inconsistent, confirmed by re-reading each view: Views/Admin/InspectorAssignments.cshtml:82 uses a text 'מחק' button; Views/Admin/Frameworks.cshtml has NO delete control at all (only an ✏️ edit button, line 64-71); Views/Admin/Institutions.cshtml has NO delete control (grep for מחק/delete/🗑️ = no matches). Trash-can icon needs to be applied consistently across all admin tables. | S |
| K15 | System tables (statuses, roles, etc.) editable only by developer | ✅ IMPLEMENTED | UserStatus/ReportStatus/UserRole/EmployeeRole are not registered in LookupController.TableDisplayNames (25-42) and have no admin CRUD controller/view — they are only seeded/changed via migrations. Effectively developer-only, satisfying the lock requirement. | S |
| K16 | Lookup/admin combos support type-to-search | 🟡 PARTIAL | **פער:** Confirmed: single-select combos are plain native <select> with no type-to-search because the global init skips non-multiple selects. Examples: educational-stage select in Frameworks add/edit (Frameworks.cshtml:102,141); inspector/program/district/sector selects on InspectorAssignments.cshtml:24,34,41,48; lookup edit modals. Extend Choices init to cover single selects on these screens. | M |

## L. Background Services / Email

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| L1 | Daily reminder service: emails employees who haven't reported or were returned-for-fix; runs periodically; every X days starting Y days before deadline; X/Y in system-constants. | ✅ IMPLEMENTED | ReminderService is a BackgroundService registered in Program.cs:77 (AddHostedService). e:\axioma-reporting\src\AxiomaReporting.Infrastructure\BackgroundJobs\ReminderService.cs runs a loop (ExecuteAsync, lines 21-40) with configurable check interval. RunReminderCycleAsync (42-129) reads system consta | S |
| L2 | Emails on report received, report approved, report rejected (with reasons), and reminder. | ✅ IMPLEMENTED | ReportStatusService.cs sends 'ReportReceived' on submit (line 91-104), 'ReportApproved' on approve (131-142), and 'ReportRejected' with RejectionReason token on reject (170-181). Templates seeded (SeedData.cs:161/170/179) and in InitialCreate migration lines 1108-1110 including the {{RejectionReason | S |
| L3 | Forgot-password email actually delivered and reset link valid (addresses TEST Sheet1 #20 'no mail', Sheet2 #2 'link broken/wrapped in Outlook'). | ✅ IMPLEMENTED | AccountController.ForgotPassword (AccountController.cs:259-317) creates a hashed PasswordResetToken, builds an ABSOLUTE reset URL via Url.Action(..., Request.Scheme) (283), and sends the 'PasswordReset' template with ResetLink + UserId tokens (285-294). EmailTemplateRenderer.FormatBodyHtml (EmailTem | S |

## M. Admin/PM Reporting for Employee

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| M1 | Manual single-report entry screen available for PM-and-above | ✅ IMPLEMENTED | ReportController.Index(userId) opens the full manual report-entry screen for any target employee (src/AxiomaReporting.Web/Controllers/ReportController.cs:59-180); CanViewEmployeeReportAsync (lines 658-667) grants SystemAdmin/ProjectManager/ProjectCoordinator access to any employee's report, and Save | S |
| M2 | Admin 'add manual report' → employee search must show the employee list | ✅ IMPLEMENTED | The Employee list is a searchable employee list rendered inside _Layout (top menu present): general search box at Views/Employee/Index.cshtml:121-123 ('חיפוש כללי', placeholder 'שם, ת.ז., קוד עובד...') plus column filters. Each row exposes a 'דווח' button (Views/Employee/Index.cshtml:391-393) → Repo | S |

## N. General / Data

| # | דרישה | סטטוס | ראיות / פער | מאמץ |
|---|-------|-------|-------------|------|
| N1 | Clean old/test reports from the database (TEST Sheet7 #2) | ❌ MISSING | **פער:** Add an idempotent SQL/py script to identify and delete test/dummy ReportRows/Reports (e.g. those seeded via database/seed-data/seed_reports.py) while preserving real data, and run it against the live DB. No such tooling or record of cleanup exists. | S |
| N2 | Script to reset ALL passwords to ID (ת.ז), matching what the employee-Excel intake should have done (TEST Sheet6 #4) | 🟡 PARTIAL | **פער:** Add a dedicated idempotent script that sets PasswordHash = bcrypt(IdNumber) and MustChangePassword = 1 for all (or a filtered set of) existing users, since import only covers newly-inserted rows. | S |
| N3 | Employee end-to-end user experience must work ('ביוזר של עובד אנחנו לא מצליחים להתנהל') (BETA #B37/F37) | 🟡 PARTIAL | **פער:** Cannot certify 'working end-to-end' without exercising the full login -> my allocations -> report -> submit flow. Needs an integrated employee walkthrough once dependent F/G/H items are resolved. | L |
| N4 | Institution/framework value shown in reports = יישוב + סמל + שם (settlement + symbol + name) (BETA #B35, #B38, #B39) | ❌ MISSING | **פער:** Compose framework display as settlement + symbol + name across report dropdown, report grid, dashboard, and Excel export, surfacing a locality on Framework (or joining via Institution by symbol) since Framework currently has no locality reference; align LookupResolver/import matching. | M |
