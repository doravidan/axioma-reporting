# Axioma Reporting - Release Notes v1.2.6

תאריך הכנה: 11/05/2026

## חבילת פריסה

קובץ:

`C:\axioma-reporting\AxiomaReporting-Delivery-v1.2.6.zip`

SHA256:

`07A7F343A19A61C307EBE594FC73A21FF8C5211226E7EFB2DC8E65D8D65E1234`

## מה תוקן בגרסה

הגרסה מטפלת בהערות שנותרו ב־Sheet4 ובנושאים החדשים מ־Sheet5 בקובץ הבדיקה.

### דשבורד דיווחים

- כפתור `ערוך` בטבלת דשבורד דיווחים הועבר לעמודה הראשונה מימין.
- פילטר סטטוס `טרם דווח` מציג עכשיו עובדים שחייבים בדיווח ועדיין לא דיווחו בתקופת הדיווח הרלוונטית.
- שורות של `טרם דווח` לא מציגות תאריך/משך מזויפים.
- תווית גודל עמוד שונתה ל־`שורות בעמוד`.
- כפתורי הפילטרים עודכנו לנוסח אחיד: `הצג` ו־`נקה`.

### דף דיווח פעילות

- עובד ללא הקצאות שנכנס לדף דיווח פעילות מקבל הודעה תקינה בעברית במקום ג'יבריש.
- העלאת אקסל לדיווח של עובד מסוים דוחה עכשיו שורות שבהן קוד העובד בקובץ שייך לעובד אחר.
- ולידציית שורת דיווח בודקת עכשיו שהערכים שנבחרו או יובאו תואמים להקצאה של העובד:
  - מחוז
  - יישוב
  - מסגרת
  - תוכנית חינוכית
  - תחום
  - נושאים
  - קוד דיון
  - כיתה/שכבה
  - מסקנות
- כך נמנעת קליטה של ערכים מטבלאות קומבו שאינם שייכים להקצאת העובד.

### איפוס סיסמה

- הודעות שגיאה במסך איפוס סיסמה תוקנו מג'יבריש לעברית.
- הודעת סיסמה לא חוקית או דומה מדי מוצגת בצורה ברורה.
- הודעת הצלחה באיפוס סיסמה מרשימת העובדים תוקנה לעברית.
- נבדק שמסך עריכת עובד נטען תקין לאחר השינויים.

## בדיקות שבוצעו לפני אריזה

- `dotnet build AxiomaReporting.sln` הסתיים בהצלחה.
- בדיקות ממוקדות עברו בהצלחה:
  - `DashboardFilterServiceTests`
  - `ReportFlowTests`
  - `ExcelImportValidationTests`
- בדיקות טעינה ידניות ב־HTTP:
  - `/Dashboard`
  - `/Dashboard?StatusId=0`
  - `/Employee/Edit/7`
  - `/Report?userId=7&allocationId=1`

## מה צריך להריץ בשרת כדי להתקין

### 1. גיבוי לפני התקנה

לפני כל החלפת גרסה:

- לבצע גיבוי מלא למסד הנתונים.
- לבצע גיבוי לתיקיית האתר הקיימת.
- לשמור בצד את קובץ הקונפיגורציה של השרת, לדוגמה:
  - `appsettings.Production.json`
  - או כל קובץ הגדרות ייעודי אחר שבו מוגדרים connection string / SMTP / נתיבי קבצים.

### 2. עצירת האתר

לעצור את האתר ב־IIS או את השירות שמריץ את האפליקציה.

דוגמה ב־IIS:

1. לפתוח IIS Manager.
2. לעצור את ה־Application Pool של האתר.
3. לעצור את האתר עצמו אם נדרש.

### 3. פריסת קבצי האפליקציה

לחלץ את:

`AxiomaReporting-Delivery-v1.2.6.zip`

בתוך החבילה קיימת תיקייה:

`app`

יש להחליף בשרת את קבצי האפליקציה הקיימים בתוכן של תיקיית `app`.

חשוב:

- לא לדרוס קובץ קונפיגורציה ייעודי של השרת בלי להשוות אותו קודם.
- אם צריך לעדכן קונפיגורציה, להשתמש בתבנית:

`config\appsettings.Production.template.json`

### 4. עדכון מסד נתונים

בגרסה `v1.2.6` אין שינוי סכימה חדש למסד הנתונים.

אם השרת כבר כולל את שינויי `v1.2.3`, אין צורך להריץ SQL נוסף.

אם השרת ישן יותר מ־`v1.2.3`, יש להריץ קודם:

`database\scripts\upgrade-v1.2.3.sql`

לאחר מכן ניתן להריץ:

`database\scripts\upgrade-v1.2.6.sql`

הקובץ `upgrade-v1.2.6.sql` הוא קובץ הצהרתי בלבד ומציין שאין שינוי DB חדש בגרסה זו.

### 5. הרשאות תיקיות

לוודא שלמשתמש שמריץ את האתר יש הרשאות כתיבה לתיקיות ההעלאה:

- `wwwroot\uploads`
- `wwwroot\uploads\attachments`
- `wwwroot\uploads\excel-errors`

### 6. הפעלת האתר

לאחר החלפת הקבצים:

1. להפעיל את ה־Application Pool.
2. להפעיל את האתר.
3. לבדוק שאין שגיאות ב־Event Viewer או בלוגים של האפליקציה.

### 7. בדיקות לאחר התקנה

לאחר שהאתר עולה:

- להתחבר כ־System Admin.
- לפתוח `/Dashboard` ולוודא שהטבלה נטענת.
- לפתוח `/Dashboard?StatusId=0` ולוודא שמוצגים עובדים שטרם דיווחו.
- לוודא שכפתור `ערוך` מופיע בעמודה הראשונה מימין.
- לפתוח דיווח קיים ולבדוק שהדף נטען.
- לבדוק העלאת אקסל עם קוד עובד אחר ולוודא שמתקבלת שגיאה ולא מתבצעת קליטה.
- לבדוק העלאת אקסל עם ערך שלא שייך להקצאת העובד ולוודא שמתקבלת שגיאה מתאימה.
- לפתוח עובד ללא הקצאות ולוודא שהודעת אין הקצאה מופיעה בעברית תקינה.
- לבדוק איפוס סיסמה לעובד ולוודא שהודעות השגיאה/הצלחה בעברית תקינה.

## הערות

- קיימות אזהרות build ידועות שאינן חוסמות פריסה:
  - הפרויקט עדיין מכוון ל־`.NET 6`, שאינו נתמך יותר רשמית.
  - קיימת אזהרת אבטחה ידועה על גרסת `MailKit`.
- מומלץ לתכנן בהמשך שדרוג ל־.NET נתמך ועדכון חבילות NuGet.
