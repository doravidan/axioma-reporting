**נושא:** עדכון מערכת דיווח עובדים אקסיומא — גרסה 1.1 וטיפול ב-20 ההערות

---

שלום רב,

מצורפת **גרסה 1.1** של מערכת דיווח עובדים אקסיומא, עם טיפול מלא ב-20 ההערות מקובץ הבדיקה ששלחתם (`בדיקת פרויקט דיווח פעילות.xlsx`).

קובץ ההתקנה: **`AxiomaReporting-Delivery-v1.1.zip`** (26 MB · SHA256: `32AD60A5…`)

---

## מה תוקן בגרסה זו

### 🔴 חוסמים שטופלו (2)

| # | תיאור | הפתרון |
|---|------|--------|
| #2 | תנאי שימוש לא הוצגו בכניסה ראשונה | תוקן ה-seed של ה-admin (לא מסומן כמי שאישר), תוכן עברי אמיתי לתנאי השימוש, ה-`RequireTermsAcceptedFilter` עם רשימת חריגים מפורשת |
| #20 | "שכחתי סיסמה" לא שלח מייל | תוקן ה-host של SMTP (`smtp@gmail.com` → `smtp.gmail.com`), הזרימה כותבת `NotificationLog` עבור כל בקשה (כולל משתמש לא קיים — בלי דליפת מידע), המייל נשלח בפועל למשתמש קיים |

### 🟠 לוגיקה עסקית (5)

| # | תיאור | הפתרון |
|---|------|--------|
| #3 | חודש פעיל ניתן לשינוי ע"י כל משתמש | רכז (Coordinator) רואה תאריך אחרון לדיווח ודיווח עתידי כשדות disabled עם 🔒. רק Admin ו-PM רשאים לשנות, עם דיאלוג אישור. |
| #8 | יום מנוחה מציג 7 ימים | רק 3 אופציות: ראשון, שישי, שבת (גם ב-UI וגם ב-validator שרת) |
| #17 | היקפי העסקה מציגים ספרות עשרוניות | אכיפת מספרים שלמים בלבד (`step="1"`), הצגה ללא `.00`, validator שרת דוחה ערכים עשרוניים |
| #19 | איפוס סיסמה ע"י Admin לא חזר לת"ז | החזרה אוטומטית לת"ז + `MustChangePassword=true` + ניקוי `FailedLoginAttempts`. כתיבה ל-`AuditLog`. הזרימה של "שכחתי סיסמה" עם טוקן במייל **לא** משתנה (המשתמש קובע סיסמה). |

### 🟡 ולידציות שרת (3)

| # | תיאור | הפתרון |
|---|------|--------|
| #5 | אין בקרת תקינות ת"ז | `IsraeliIdValidator` חדש עם אלגוריתם ספרת ביקורת רשמי. מוטמע ב-`UserValidator`. כיסוי ב-10 בדיקות יחידה. |
| #6 | אין בקרת תקינות טלפון | Regex לטלפון ישראלי (קווי + נייד), אופציונלי |
| #7 | הודעות שגיאה באנגלית | סקירה מלאה של כל ה-`WithMessage` ב-FluentValidation וכל ה-`[Required]/[StringLength]` ב-Models — הכל עברית |

### 🔵 UI / רשימות (10)

| # | תיאור | הפתרון |
|---|------|--------|
| #1 | להתאים את העיצוב לצבעי הלוגו | דגימה אוטומטית של 3 צבעי המותג מ-`logo.png` (כתום `#F1601F`, נייבי `#1D144D`, מארון `#7D3739`). theme.css עם CSS variables. |
| #4 | אין תפריט גרפי כפי שבמפרט | מסך חדש `/MyAllocations` עם באנר חודש דיווח פעיל ושני אריחים גדולים (עדכון פעילות + העלאת אקסל). הקישור בנאבבר עבור עובדים מנותב לכאן. |
| #9 + #12 | טבלאות תפקידים וסטטוסים באנגלית | מיגרציית DB חדשה: עמודה `DescriptionHebrew` ב-`UserRoles` ו-`UserStatuses`, מאוכלסת בעברית. כל ה-views מציגים את הערך העברי. |
| #10 | חסרה עמודת תיאור בייבוא קובץ | טבלת תוצאות `BatchReportImport` עם עמודת "תיאור" עברית לכל שורה (התאמה / עדכון / דילוג / דחייה) |
| #11 | צבעי כרטיס בכותרת ולא ברקע | רקע צבעוני (`bg-primary` כחול / `bg-success` ירוק) על `card-header`. כותרת רגילה: "עריכת עובד" / "עריכת הקצאה". |
| #13 | כפתורי פעולה לא נראים בלי גלילה | עמודת הפעולות הועברה לימין (RTL-first), ללא צורך בגלילה |
| #14 | פילטר לא נשמר אחרי פעולה | כל POST → Redirect משמר את ערכי הפילטר ב-querystring דרך helper מרוכז |
| #15 | חסרים פילטרים ברשימת עובדים | רשימת עובדים מקבלת 16 פילטרים זהים לרשימת ההקצאות (כולל מחוז, מגזר, תוכנית, יום מנוחה, נעילה וכו') |
| #16 | לא כל העמודות ניתנות למיון | כל header של עמודה הוא קישור מיון עם `aria-sort` |
| #18 | בחירה מרובה לא ברורה | התקנת **Choices.js** — widget tag-style מודרני עם chips, חיפוש, ו-RTL |

### תיקון תשתית (פתר בעיית assets שלא נטענו)

`Login.cshtml` היה עם `Layout = null` ולא טען `site.css`/`site.js`. הוקם **`_AnonymousLayout`** משותף לכל מסכי הכניסה והשחזור (`Login`, `ForgotPassword`, `ResetPassword`, `ChangePassword`, `TermsOfUse`, `TwoFactor`) שטוען אותם נכסים בדיוק שטוענים מסכי המערכת — כולל הודעות ולידציה בעברית.

### מדדי איכות

- **0 שגיאות בנייה** ב-Release.
- **321 בדיקות אוטומטיות עוברות** (גידול מ-240 בגרסה 1.0 — נוספו 81 בדיקות חדשות לכיסוי כל התיקונים).
- בנוסף ל-20 ההערות הממוספרות, הוטמעה גם הערה בלתי-ממוספרת מהדף: **"קוד עובד ספרות בלבד"** — ולידציה בצד שרת + DTO + הטופס, עם סבילות לעריכת רשומות ישנות בעלות קוד אלפא-נומרי.
- 12 קבצים חדשים, 28 קבצים שונו.
- בנוסף לעבודת הסוכנים המקבילים, בוצע **מעבר אינטגרציה** שסגר פערי תפר נוספים: ולידטורים שלא נקראו בפועל ב-POST, ולידציה client-side שהייתה חסרה, אריח ה-Excel ב-`/MyAllocations` שהפנה ל-action בלעדי-POST, ושמירת פילטר במחלקה גדולה יותר של פעולות שורה.

---

## הוראות התקנה (שדרוג מ-v1.0)

### שלב 0 — גיבוי (חובה לפני כל פעולה)

```powershell
# גיבוי DB
sqlcmd -S .\SQLEXPRESS -Q "BACKUP DATABASE AxiomaReporting
  TO DISK = N'D:\backups\AxiomaReporting-pre-v1.1.bak' WITH INIT"

# גיבוי תיקיית האפליקציה
robocopy C:\inetpub\AxiomaReporting D:\backups\app-pre-v1.1 /MIR
```

### שלב 1 — עצירת האפליקציה

```powershell
Import-Module WebAdministration
Stop-WebAppPool -Name "AxiomaReporting"
```

### שלב 2 — מיגרציית DB (חובה — יש שינוי סכמה)

```powershell
sqlcmd -S .\SQLEXPRESS -d AxiomaReporting -E -i database\schema.sql
```

המיגרציה החדשה (`20260430110451_AddHebrewDescriptionsToRolesAndStatuses`) מוסיפה:
- עמודת `DescriptionHebrew` ל-`UserRoles` ול-`UserStatuses` ומאכלסת אותה בעברית
- מחליפה את גוף תנאי השימוש בטקסט עברי אמיתי
- מסירה את ה-`TermsOfUseAcceptance` של ה-Admin (כך שתנאי השימוש מוצגים בכניסה הראשונה)

הסקריפט idempotent — בטוח להריץ פעמים.

### שלב 3 — החלפת קבצי האפליקציה

חלץ את `app/` מ-`AxiomaReporting-Delivery-v1.1.zip` והחלף את התוכן של `C:\inetpub\AxiomaReporting\`.

**אל תדרוס** את הקבצים הבאים:
- `appsettings.Production.json` — מכיל את סיסמת ה-DB
- `wwwroot/uploads/` — קבצים מצורפים של עובדים

### שלב 4 — הפעלת האפליקציה

```powershell
Start-WebAppPool -Name "AxiomaReporting"
```

### שלב 5 — בדיקת תקינות (10 דקות)

לפי `docs/DEPLOY_CHECKLIST.md`:

- [ ] גלוש לכתובת — מסך כניסה נטען עם הצבעים החדשים
- [ ] התחברות כ-Admin — **תנאי שימוש מוצגים** (חוסם #2 הוסר)
- [ ] `/Admin/EmailServerSettings` → "שלח מייל בדיקה" → המייל מתקבל
- [ ] `/Account/ForgotPassword` עבור משתמש קיים → המייל מתקבל (חוסם #20 הוסר)
- [ ] `/Employee` — 16 פילטרים נראים, כפתורי פעולה בימין, multi-select מציג כ-chips
- [ ] `/MyAllocations` — באנר חודש פעיל + 2 אריחים
- [ ] `/Admin/ReportingMonths` כרכז — תאריך אחרון נעול עם 🔒
- [ ] איפוס סיסמה לעובד → הסיסמה היא ת"ז + נדרש שינוי מאולץ

---

## פתרונות חירום

| תרחיש | פתרון |
|-------|------|
| משהו השתבש בעלייה לאוויר | חזרה לשלב 0 → שחזור DB מ-`pre-v1.1.bak`, החזרת התיקייה מ-`app-pre-v1.1` |
| תנאי שימוש חוסמים את ה-Admin | `UPDATE Users SET AcceptedTermsOfUse=1 WHERE IdNumber='ADMIN'` (זמני) |
| Choices.js לא נטען | בדוק שהקובץ `wwwroot/lib/choices.js/choices.min.js` קיים והרשאות תקינות |
| מייל בדיקה נכשל | בדוק `/Admin/NotificationLogs` → `FailureReason`. ודא שהפורט יוצא בחומת אש |

---

## נושאים פתוחים שייוודעו רק בעלייה אצלכם

- **בדיקת SMTP מקצה לקצה** — דורש את אישורי ה-relay הסופיים שלכם. תיוודא בעת ההתקנה.
- **בדיקת ביצועים עם נתוני אמת** — אם ייתגלו אינדקסים חסרים בעת שימוש בשטח, נוסיף אותם בעדכון מינורי.
- **התאמת צבעי המותג** — דגמתי 3 צבעים מהלוגו. אם יש לכם guideline מעודכן עם הקסים מדויקים, נחליף את 3 ה-CSS variables ב-`theme.css`.

---

## תמיכה

- מסמך התקנה מלא: `docs/CLIENT_DELIVERY.md`
- רשימת תיוג: `docs/DEPLOY_CHECKLIST.md`
- מדריך תפעול שוטף: `docs/OPERATIONS.md`
- דו"ח הוורות גרסה זו: `VERIFICATION_REPORT_v1.1.md` (מצורף בנפרד)

זמין/ה לכל שאלה ולתיאום חלון התקנה (~30 דקות לתהליך השדרוג עצמו, פלוס 10 דקות לבדיקת תקינות).

בברכה,

*[שם שולח]*
*פרויקט אקסיומא — דיווח עובדים*
*2026-04-30*
